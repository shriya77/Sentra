# Firebase: for per-user storage, backend must verify the frontend's ID token.
# Download a service account key from Firebase Console → Project Settings → Service accounts → Generate new private key.
# Then either set the path to the JSON file, or paste the JSON (minified) as a single line.
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/firebase-service-account.json
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"sentra-10085",...}

# Optional: path to SQLite database (default: backend/data/sentra.db)
# SENTRA_DB_PATH=/path/to/sentra.db

# Optional: 11Labs API key for voice reflection (leave unset to disable voice)
# ELEVENLABS_API_KEY=your_api_key_here

# Optional: OpenAI API key for AI-powered insights and signal descriptions (leave unset to use template-based fallback)
# OPENAI_API_KEY=sk-...
